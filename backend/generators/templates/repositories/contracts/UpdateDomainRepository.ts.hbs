import { Query } from "@/application/@types";
import { {{pascalCase name}}Data } from "@/slices/{{camelCase name}}/entities";

export interface Update{{pascalCase name}}Repository {
    update{{pascalCase name}}: (query: Query) => Promise<{{pascalCase name}}Data | null>;
}
