import { Query } from "@/application/@types";
import { {{pascalCase name}}PaginatedData } from "@/slices/{{camelCase name}}/entities";

export interface Load{{pascalCase name}}ByPageRepository {
    loadByPage(query: Query): Promise<{{pascalCase name}}PaginatedData | null>;
}
