import { Query } from "@/application/@types";
import { {{pascalCase name}}Data } from "@/slices/{{camelCase name}}/entities";
import { Load{{pascalCase name}}Repository } from "@/slices/{{camelCase name}}/repositories";

export type load{{pascalCase name}} = (query: Query) => Promise<{{pascalCase name}}Data | null>;

export type load{{pascalCase name}}Signature = (load{{pascalCase name}}: Load{{pascalCase name}}Repository) => load{{pascalCase name}};

export const load{{pascalCase name}}Usecase: load{{pascalCase name}}Signature =
    (load{{pascalCase name}}Repository: Load{{pascalCase name}}Repository) => (query: Query) => {
        return load{{pascalCase name}}Repository.load{{pascalCase name}}(query);
    };
