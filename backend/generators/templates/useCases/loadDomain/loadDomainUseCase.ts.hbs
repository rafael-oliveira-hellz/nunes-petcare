import { {{pascalCase name}}Data, {{pascalCase name}}Entity } from "@/slices/{{camelCase name}}/entities";
import { Load{{pascalCase name}}Repository } from "@/slices/{{camelCase name}}/repositories";

export type load{{pascalCase name}} = (data: {{pascalCase name}}Data) => Promise<{{pascalCase name}}Entity | null>;

export type load{{pascalCase name}}Signature = (load{{pascalCase name}}: Load{{pascalCase name}}Repository) => load{{pascalCase name}};

export const load{{pascalCase name}}Usecase: load{{pascalCase name}}Signature =
    (load{{pascalCase name}}: Load{{pascalCase name}}Repository) => (data: {{pascalCase name}}Data) => {
        return load{{pascalCase name}}.load{{pascalCase name}}(new {{pascalCase name}}Entity(data));
    };
