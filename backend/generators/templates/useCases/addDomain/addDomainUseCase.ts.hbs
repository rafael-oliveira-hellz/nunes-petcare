import { {{pascalCase name}}Data, {{pascalCase name}}Entity } from "@/slices/{{camelCase name}}/entities";
import { Add{{pascalCase name}}Repository } from "@/slices/{{camelCase name}}/repositories";

export type add{{pascalCase name}} = (data: {{pascalCase name}}Data) => Promise<{{pascalCase name}}Entity | null>;

export type add{{pascalCase name}}Signature = (add{{pascalCase name}}: Add{{pascalCase name}}Repository) => add{{pascalCase name}};

export const add{{pascalCase name}}Usecase: add{{pascalCase name}}Signature =
    (add{{pascalCase name}}: Add{{pascalCase name}}Repository) => (data: {{pascalCase name}}Data) => {
        return add{{pascalCase name}}.add{{pascalCase name}}(new {{pascalCase name}}Entity(data));
    };
